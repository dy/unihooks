import t from 'tape'
import { useFormField } from '../src/index'

t('useFormField: must control existing input', t => {
  t.end()
})

t('useFormField: must be able to create new input', t => {
  t.end()
})

t('useFormField: must be able to manage value', t => {
  t.end()
})

t('useFormField: must be able to validate value', t => {
  t.end()
})
